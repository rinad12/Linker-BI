# Postgres (matches docker-compose.yml defaults)
POSTGRES_USER=linker
POSTGRES_PASSWORD=linker
POSTGRES_DB=linker_bi
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# LangGraph checkpointer connection string
# Used by langgraph.checkpoint.postgres.PostgresSaver
DATABASE_URL=postgresql://linker:linker@localhost:5432/linker_bi

# LLM
OPENAI_API_KEY=
