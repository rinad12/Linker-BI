services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-linker}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-linker}
      POSTGRES_DB: ${POSTGRES_DB:-linker_bi}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-linker} -d ${POSTGRES_DB:-linker_bi}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
